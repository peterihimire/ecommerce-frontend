import React from "react";
import "./styles.scss";

const CartInfo = () => {
  return (
    <section className={`cart-info`}>
      <div className="wrapper">
        <div className={`title`}>
          <h2>My Cart</h2>
        </div>
      </div>
    </section>
  );
};

export default CartInfo;
